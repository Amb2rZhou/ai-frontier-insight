anthropic>=0.40.0
openai>=1.0.0
requests>=2.28.0
feedparser>=6.0.0
pyyaml>=6.0.0
openpyxl>=3.1.0
# SOCKS 代理支持：requests 走 PySocks，openai/httpx 走 socksio
# 没装时若 daily_brief.sh 检测到代理并设 ALL_PROXY=socks5h://，openai SDK 会在
# 构造 client 时抛 ImportError，导致信号抽取整条 pipeline 失败（见 2026-06-09 事故）
PySocks>=1.7.1
httpx[socks]>=0.27.0
